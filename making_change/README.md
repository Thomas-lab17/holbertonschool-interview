# Making Change

Determine the fewest number of coins needed to meet a given total.

## File
- `0-making_change.py`: implements `makeChange(coins, total)`. Returns 0 if
  total is 0 or less, -1 if the total cannot be met.
