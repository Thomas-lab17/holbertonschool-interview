# Lockboxes

## Task
Implement `canUnlockAll(boxes)` to determine if all boxes can be opened.

- Boxes are numbered `0` to `n - 1`
- `boxes[0]` is unlocked
- A key with value `k` opens box `k`
- Keys outside the range `0..n-1` are ignored

## File
- `0-lockboxes.py`
