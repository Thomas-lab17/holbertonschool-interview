# Prime Game

Determine the winner of the prime game where players remove primes and their
multiples from a set of consecutive integers 1..n, playing optimally.

## File
- `0-prime_game.py`: implements `isWinner(x, nums)` returning the name of the
  player who won the most rounds, or `None` if tied.
